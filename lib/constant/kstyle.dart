import 'package:flutter/material.dart';

const kstyle =
    TextStyle(color: Colors.white70, fontSize: 15, letterSpacing: 0.4);

final kborder = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [Colors.lightBlue.shade100, Colors.lightGreen.shade100]);

final whiteColor = TextStyle(color: Colors.white);
